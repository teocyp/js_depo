// It merges (combines) two or more arrays, it doesn't change the arrays, instead returns a new array.

const array1 = ['a', 'b', 'c'];
const array2 = ['d', 'e', 'f'];
const concat = array1.concat(array2);

console.log(concat); //outputs ['a', 'b', 'c', 'd', 'e', 'f',]


