// unshift() method adds one or more elements to the beginning of an array and returns the new length of the array.

const arr = [1, 2, 3];

console.log(arr.unshift(4, 5, 'arrNew')); // outputs [4,5,'arrNew',1,2,3]

console.log(arr);