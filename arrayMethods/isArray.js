// isArray method determines wether the passed value is an array


const arr = [1, 2, 3];
const obj = { a: 1, b: 2, c: 3 };

console.log(Array.isArray(arr)); //returns true
console.log(Array.isArray(obj)); //returns false