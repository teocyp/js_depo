// reverse() method reverses an array in place. The first array element becomes the last and vice-versa.

const numbers = [1, 2, 3, 4, 5, 6, 7];

const reversedNum = numbers.reverse();

console.log(reversedNum); // outputs [7, 6, 5, 4, 3, 2, 1]