// As same as for loop, only the syntax is slightly different..
// We declare i outside the loop..
const names = ['Teo', 'Mun', 'Bekir'];
let i = 0;

while (i < names.length) {
      console.log(names[i]);
      i++;
}

